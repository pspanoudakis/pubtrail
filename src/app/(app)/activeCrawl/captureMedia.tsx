import { ReactElement } from "react";
import { CaptureMediaPresenter } from "@/presenters/captureMediaPresenter";

export default function CaptureMediaPage(): ReactElement {
    return <CaptureMediaPresenter />;
}
